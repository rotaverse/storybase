const initialState = 0;

const reducer = (
   state = initialState,
   action
) = {
  switch (action.type) {
     case "login":
        return state = action.payload; {/* serialize loginName, password */}



  }




}